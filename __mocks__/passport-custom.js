// Stub for passport-custom — not installed in this project; mocked to allow Jest to run
const Strategy = class Strategy {
  constructor(name, verify) {}
};

module.exports = { Strategy };
module.exports.default = { Strategy };
